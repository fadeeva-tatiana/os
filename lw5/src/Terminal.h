#pragma once
#include <string>

namespace Terminal {
    const auto PROG = "PROG";
    const auto ID = "ID";
    const auto BEGIN = "BEGIN";
    const auto END = "END";
    const auto VAR = "VAR";
    const auto INT = "INT";
    const auto FLOAT = "FLOAT";
    const auto BOOL = "BOOL";
    const auto STRING = "STRING";
    const auto READ = "READ";
    const auto WRITE = "WRITE";
    const auto NUM = "NUM";

    const auto COLON = ":";
    const auto SEMICOLON = ";";
    const auto COMMA = ",";
    const auto OPENING_BRACKET = "(";
    const auto CLOSING_BRACKET = ")";
    const auto ASSIGNMENT = ":=";
    const auto PLUS = "+";
    const auto MULTIPLY = "*";
    const auto MINUS = "-";
};// namespace Terminal
